import React from "react";
import { View, Text } from "react-native";

const PagerViewMock = () => (
	<View>
		<Text>PagerView is not available on web.</Text>
	</View>
);

export default PagerViewMock;
