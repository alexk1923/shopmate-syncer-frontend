import { View, Text, TextInputComponent, TextInput } from "react-native";
import React from "react";

const register = () => {
	return (
		<View>
			<TextInput>sdsd</TextInput>
		</View>
	);
};

export default register;
